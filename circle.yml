machine:
  enviroment:
    PATH: ~/.roswell/bin:$PATH

dependencies:
  pre:
    - curl -L https://raw.githubusercontent.com/snmsts/roswell/release/scripts/install-for-ci.sh | sh;
    - case $CIRCLE_NODE_INDEX in
        0) ros config set default.lisp sbcl-bin ;;
        1) ros install ccl-bin;
           ros config set default.lisp ccl-bin ;;
      esac
    - ros run -- --version
  override:
    - ros -l ~/lisp/prove/prove.asd install prove

test:
  override:
    - if [ "$CIRCLE_NODE_INDEX" = 0 ]; then COVERALLS=true run-prove cl-bson-test.asd; else run-prove cl-bson-test; fi: {parallel: true}
